//// common.c
//#include "common.h"
//
//unsigned char clampUChr(unsigned char v, unsigned char min, unsigned char max) { return v < min ? min : (v > max ? max : v); }
//int clampInt(int v, int min, int max) { return v < min ? min : (v > max ? max : v); }
//double clampDbl(double v, double min, double max) { return v < min ? min : (v > max ? max : v); }
